# 서버 인프라 학습 가이드 (주니어 개발자용)

> TORI COFFEE 프로젝트를 기반으로 한 Kubernetes & AWS 인프라 학습 문서
> 이 문서는 실제 프로젝트 설정을 예시로 들어 설명합니다.

---

## 목차

1. [기본 개념 이해하기](#1-기본-개념-이해하기)
2. [왜 DB를 4개로 분리했을까?](#2-왜-db를-4개로-분리했을까)
3. [Kubernetes 핵심 개념](#3-kubernetes-핵심-개념)
4. [AWS EKS 구성요소](#4-aws-eks-구성요소)
5. [HPA (자동 스케일링)](#5-hpa-자동-스케일링)
6. [Ingress와 로드밸런싱](#6-ingress와-로드밸런싱)
7. [서비스 간 통신](#7-서비스-간-통신)
8. [리소스 관리](#8-리소스-관리)
9. [데이터 영속성 (PV/PVC)](#9-데이터-영속성-pvpvc)
10. [ConfigMap과 Secret](#10-configmap과-secret)
11. [헬스 체크 (Probe)](#11-헬스-체크-probe)
12. [실제 트래픽 흐름](#12-실제-트래픽-흐름)

---

## 1. 기본 개념 이해하기

### 1.1 전통적인 서버 vs 컨테이너

```
[전통적인 방식]
┌─────────────────────────────┐
│         물리 서버            │
│  ┌───────────────────────┐  │
│  │    운영체제 (Linux)     │  │
│  │  ┌─────┐ ┌─────┐     │  │
│  │  │App1 │ │App2 │     │  │
│  │  └─────┘ └─────┘     │  │
│  └───────────────────────┘  │
└─────────────────────────────┘

문제점:
- App1이 라이브러리 A v1.0 필요
- App2가 라이브러리 A v2.0 필요
- 충돌 발생! 😱
```

```
[컨테이너 방식]
┌─────────────────────────────┐
│         물리 서버            │
│  ┌───────────────────────┐  │
│  │    운영체제 (Linux)     │  │
│  │    Docker Engine       │  │
│  │  ┌─────┐ ┌─────┐     │  │
│  │  │컨테이너│ │컨테이너│     │  │
│  │  │App1 │ │App2 │     │  │
│  │  │v1.0 │ │v2.0 │     │  │
│  │  └─────┘ └─────┘     │  │
│  └───────────────────────┘  │
└─────────────────────────────┘

해결:
- 각 컨테이너가 독립적인 환경
- 서로 영향 없음! 😊
```

### 1.2 주요 용어 정리

| 용어 | 쉬운 설명 | 비유 |
|------|----------|------|
| **컨테이너** | 앱 + 실행환경을 패키징한 것 | 이사할 때 쓰는 박스 📦 |
| **이미지** | 컨테이너를 만들기 위한 설계도 | 붕어빵 틀 🐟 |
| **Docker** | 컨테이너를 만들고 실행하는 도구 | 붕어빵 굽는 기계 |
| **Kubernetes (K8s)** | 여러 컨테이너를 관리하는 도구 | 붕어빵 가게 사장님 👨‍💼 |
| **클러스터** | K8s가 관리하는 서버들의 집합 | 붕어빵 가게 체인점 전체 |
| **노드** | 클러스터를 구성하는 개별 서버 | 각 체인점 매장 🏪 |
| **파드 (Pod)** | K8s에서 배포하는 최소 단위 (컨테이너 1개 이상) | 붕어빵 한 판 |

### 1.3 우리 프로젝트 구조

```
[TORI COFFEE 전체 구조]

사용자 (브라우저)
       │
       ▼
┌─────────────────────────────────────────────────────────┐
│                    AWS Cloud                            │
│  ┌───────────────────────────────────────────────────┐  │
│  │              EKS Cluster (teamproject-cluster)     │  │
│  │                                                    │  │
│  │   ┌─────────┐  ┌─────────┐  ┌─────────┐          │  │
│  │   │ Node 1  │  │ Node 2  │  │ Node 3  │  ...     │  │
│  │   │(EC2)    │  │(EC2)    │  │(EC2)    │          │  │
│  │   │         │  │         │  │         │          │  │
│  │   │ [Pods]  │  │ [Pods]  │  │ [Pods]  │          │  │
│  │   └─────────┘  └─────────┘  └─────────┘          │  │
│  │                                                    │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌─────────┐  ┌─────────┐                              │
│  │   ECR   │  │   ALB   │                              │
│  │(이미지) │  │(로드밸런서)│                              │
│  └─────────┘  └─────────┘                              │
└─────────────────────────────────────────────────────────┘
```

---

## 2. 왜 DB를 4개로 분리했을까?

### 2.1 MSA (Microservices Architecture)의 핵심 원칙

```
[모놀리식 (전통적인 방식)]

┌────────────────────────────────┐
│          하나의 애플리케이션      │
│  ┌──────┐ ┌──────┐ ┌──────┐  │
│  │ 회원  │ │ 상품  │ │ 주문  │  │
│  └──┬───┘ └──┬───┘ └──┬───┘  │
│     └────────┼────────┘      │
│              ▼               │
│      ┌─────────────┐         │
│      │   하나의 DB   │         │
│      │  (모든 테이블) │         │
│      └─────────────┘         │
└────────────────────────────────┘

문제점:
❌ 회원 기능 수정하면 전체 재배포
❌ 주문이 폭주하면 전체 서버 증설 필요
❌ DB 장애 = 전체 서비스 장애
❌ 팀 간 코드 충돌 빈번
```

```
[MSA (우리 프로젝트)]

┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ member  │ │ product │ │  order  │ │inventory│
│ service │ │ service │ │ service │ │ service │
└────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘
     │           │           │           │
     ▼           ▼           ▼           ▼
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│member_db│ │product_ │ │order_db │ │inventory│
│         │ │   db    │ │         │ │   _db   │
└─────────┘ └─────────┘ └─────────┘ └─────────┘

장점:
✅ 회원 기능만 수정 → 회원 서비스만 재배포
✅ 주문 폭주 → 주문 서비스만 증설 (HPA!)
✅ member_db 장애 → 회원만 안됨, 나머지 정상
✅ 팀별 독립 개발 가능
```

### 2.2 우리 프로젝트의 DB 구성

| DB 이름 | 담당 서비스 | 저장 데이터 | 분리 이유 |
|---------|------------|------------|----------|
| `member_db` | member-service | 회원, 인증, 토큰 | 인증은 보안이 중요, 독립 관리 |
| `product_db` | product-service | 메뉴, 카테고리, 옵션 | 읽기가 많음, 캐싱 최적화 가능 |
| `order_db` | order-service | 주문, 장바구니 | 쓰기가 많음, 트랜잭션 집중 |
| `inventory_db` | inventory-service | 재고, 원재료, 레시피 | 실시간 재고 관리, 동시성 제어 |

### 2.3 실제 K8s 설정 (mysql-product.yaml 예시)

```yaml
# k8s/mysql/mysql-product.yaml

apiVersion: apps/v1
kind: StatefulSet          # ⚠️ Deployment가 아닌 StatefulSet 사용
metadata:
  name: mysql-product
  namespace: tori-app
spec:
  serviceName: mysql-product   # 고정된 네트워크 이름
  replicas: 1
  selector:
    matchLabels:
      app: mysql-product
  template:
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_DATABASE
              value: "product_db"      # 이 DB만 생성
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:          # 비밀번호는 Secret에서
                  name: db-secrets
                  key: MYSQL_PRODUCT_ROOT_PASSWORD
          volumeMounts:
            - name: mysql-storage
              mountPath: /var/lib/mysql  # 데이터 저장 위치
  volumeClaimTemplates:
    - metadata:
        name: mysql-storage
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi              # 10GB 볼륨 요청
```

**왜 StatefulSet인가?**

| Deployment | StatefulSet |
|------------|-------------|
| 파드 이름이 랜덤 (pod-abc123) | 파드 이름이 고정 (mysql-product-0) |
| 순서 없이 생성/삭제 | 순서대로 생성, 역순으로 삭제 |
| 저장소 공유 가능 | 각 파드가 고유한 저장소 |
| 웹 서버에 적합 | **데이터베이스에 적합** ✅ |

---

## 3. Kubernetes 핵심 개념

### 3.1 Pod (파드)

```
K8s에서 배포하는 가장 작은 단위

┌─────────────────────────────┐
│           Pod               │
│  ┌───────────┐             │
│  │ Container │  ← 보통 1개  │
│  │ (Spring)  │             │
│  └───────────┘             │
│                             │
│  IP: 10.0.0.15             │
│  Port: 8005                │
└─────────────────────────────┘

특징:
- 파드 내 컨테이너들은 같은 IP 공유
- 파드는 언제든 죽을 수 있음 (비영구적)
- 죽으면 새 파드가 새 IP로 생성됨
```

### 3.2 Deployment (디플로이먼트)

```yaml
# k8s/services/frontend-service.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-service
  namespace: tori-app
spec:
  replicas: 1                    # 파드 개수 (HPA가 조절함)
  selector:
    matchLabels:
      app: frontend-service      # 이 라벨을 가진 파드를 관리
  template:                      # 파드 템플릿
    metadata:
      labels:
        app: frontend-service    # 파드에 붙는 라벨
    spec:
      containers:
        - name: frontend-service
          image: 490866675691.dkr.ecr.../frontend-service:latest
          ports:
            - containerPort: 8005
```

**Deployment가 하는 일:**

```
[Deployment: frontend-service]
        │
        │ "replicas: 3 유지해!"
        │
        ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│  Pod 1  │ │  Pod 2  │ │  Pod 3  │
│(정상 ✅)│ │(죽음 ❌)│ │(정상 ✅)│
└─────────┘ └─────────┘ └─────────┘
                │
                ▼
        "어? Pod 2 죽었네?"
        "새 Pod 2 만들어야지!"
                │
                ▼
┌─────────┐ ┌─────────┐ ┌─────────┐
│  Pod 1  │ │ Pod 2'  │ │  Pod 3  │
│(정상 ✅)│ │(새로생성)│ │(정상 ✅)│
└─────────┘ └─────────┘ └─────────┘
```

### 3.3 Service (서비스)

```
파드는 IP가 계속 바뀌는데, 어떻게 찾지? → Service가 해결!

┌─────────────────────────────────────┐
│     Service: frontend-service       │
│     ClusterIP: 10.100.50.25        │
│     Port: 8005                      │
│                 │                   │
│    ┌────────────┼────────────┐     │
│    ▼            ▼            ▼     │
│ ┌──────┐   ┌──────┐   ┌──────┐    │
│ │Pod 1 │   │Pod 2 │   │Pod 3 │    │
│ │.0.10 │   │.0.11 │   │.0.12 │    │
│ └──────┘   └──────┘   └──────┘    │
└─────────────────────────────────────┘

다른 서비스에서 호출할 때:
http://frontend-service:8005  ← 이름으로 호출 가능!
(K8s DNS가 자동으로 IP 찾아줌)
```

```yaml
# Service 정의

apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: tori-app
spec:
  selector:
    app: frontend-service    # 이 라벨을 가진 파드들에게 트래픽 전달
  ports:
    - port: 8005             # Service 포트
      targetPort: 8005       # 파드의 포트
```

### 3.4 Namespace (네임스페이스)

```
클러스터 내에서 리소스를 논리적으로 분리

┌─────────────────────────────────────────────┐
│              Kubernetes Cluster              │
│                                             │
│  ┌─────────────────┐  ┌─────────────────┐  │
│  │ Namespace:       │  │ Namespace:       │  │
│  │ tori-app        │  │ kube-system      │  │
│  │                 │  │                 │  │
│  │ - frontend      │  │ - coredns       │  │
│  │ - member        │  │ - aws-lb-ctrl   │  │
│  │ - product       │  │ - ebs-csi       │  │
│  │ - mysql들       │  │                 │  │
│  └─────────────────┘  └─────────────────┘  │
│                                             │
│  우리 앱               K8s 시스템 컴포넌트    │
└─────────────────────────────────────────────┘
```

---

## 4. AWS EKS 구성요소

### 4.1 EKS (Elastic Kubernetes Service)

```
AWS가 관리해주는 Kubernetes

[직접 K8s 설치]                    [EKS 사용]

Master Node 직접 관리 😰           AWS가 관리 😊
- etcd 백업                       - 자동 백업
- API Server 관리                 - 자동 업데이트
- 장애 시 직접 복구                - 자동 복구

→ 우리는 Worker Node만 신경쓰면 됨!
```

### 4.2 EC2 (Worker Node)

```yaml
# k8s/cluster-config.yaml

managedNodeGroups:
  - name: tori-nodes
    instanceType: t3.medium      # 인스턴스 타입
    desiredCapacity: 5           # 원하는 노드 수
    minSize: 3                   # 최소 노드 수
    maxSize: 7                   # 최대 노드 수
```

**t3.medium 스펙:**
| 항목 | 값 |
|------|-----|
| vCPU | 2개 |
| 메모리 | 4GB |
| 네트워크 | 중간 |
| 시간당 비용 | ~$0.05 (서울) |

**인스턴스 타입 비교:**
| 타입 | vCPU | 메모리 | 시간당 비용 | 용도 |
|------|------|--------|------------|------|
| t3.micro | 2 | 1GB | $0.013 | 테스트용 (너무 작음) |
| t3.small | 2 | 2GB | $0.026 | 소규모 (부족할 수 있음) |
| **t3.medium** | 2 | 4GB | $0.052 | **우리 프로젝트 선택** ✅ |
| t3.large | 2 | 8GB | $0.104 | 여유 있음 |
| t3.xlarge | 4 | 16GB | $0.208 | 대규모 |

**인스턴스 선택 시 주의사항:**
```
[t3.micro / t3.small 사용 시 문제]

문제 1: 메모리 부족
┌─────────────────────────────────┐
│ Node (t3.small - 2GB)           │
│                                 │
│ 시스템 예약: ~500MB              │
│ kubelet: ~100MB                 │
│ 남은 공간: ~1.4GB               │
│                                 │
│ Spring Boot 파드 1개: ~400MB    │
│ → 3개만 띄워도 메모리 부족! 💥   │
└─────────────────────────────────┘

문제 2: CPU 쓰로틀링
- t3는 "버스트 가능" 인스턴스
- CPU 크레딧 소진 시 성능 급감
- 지속적인 고부하에 부적합

문제 3: 파드 개수 제한
- 인스턴스 타입별로 최대 파드 수 제한
- t3.micro: 최대 4개 파드
- t3.small: 최대 11개 파드
- t3.medium: 최대 17개 파드
```

**우리가 겪었던 이슈:**
```
[이슈 1: Insufficient memory]

상황: 노드 2개로 시작 → 파드들이 Pending 상태

원인:
- 9개 서비스 + 4개 MySQL + RabbitMQ + 시스템 파드
- 노드 2개로는 메모리 부족

해결:
- 노드 수를 5개로 증가
- 또는 requests.memory를 줄이기

kubectl describe pod <파드명> -n tori-app
# Events:
#   Warning  FailedScheduling  0/2 nodes available: 2 Insufficient memory
```

```
[이슈 2: 노드 개수 vs 인스턴스 크기]

선택지 A: 큰 인스턴스 적게 (t3.large x 2)
- 비용: $0.104 x 2 = $0.208/시간
- 장점: 관리 간단
- 단점: 노드 1개 죽으면 50% 다운

선택지 B: 작은 인스턴스 많이 (t3.medium x 5) ✅ 우리 선택
- 비용: $0.052 x 5 = $0.260/시간
- 장점: 고가용성 (1개 죽어도 20%만 영향)
- 단점: 관리할 노드 많음

∴ 프로덕션에서는 작은 인스턴스를 여러 개 쓰는 게 안전!
```

```
[이슈 3: EBS CSI Driver 미설치]

상황: MySQL 파드가 Pending 상태로 멈춤

원인:
- EKS 1.23부터 EBS 볼륨 사용 시 CSI Driver 필수
- 기본 설치 안 됨!

해결:
eksctl create addon --name aws-ebs-csi-driver --cluster teamproject-cluster --region ap-northeast-2 --force

확인:
kubectl get pods -n kube-system -l app.kubernetes.io/name=aws-ebs-csi-driver
```

**왜 5개 노드인가?**
```
우리 파드 구성:
- 애플리케이션: 9개 서비스
- MySQL: 4개
- RabbitMQ: 1개
- 시스템 파드: ~10개 (CoreDNS, CNI 등)

총 약 24개 파드를 5개 노드에 분산
→ 노드당 평균 4~5개 파드
→ 한 노드 죽어도 다른 노드가 커버 가능
```

### 4.3 ECR (Elastic Container Registry)

```
Docker Hub의 AWS 버전 (프라이빗 이미지 저장소)

[개발 PC]                         [ECR]                          [EKS]
   │                                │                               │
   │  docker build                  │                               │
   │  docker tag                    │                               │
   │  docker push ─────────────────▶│                               │
   │                                │                               │
   │                                │◀───────── docker pull ────────│
   │                                │                               │

저장소 URL 형식:
{AWS계정ID}.dkr.ecr.{리전}.amazonaws.com/{앱이름}:{태그}

우리 프로젝트:
490866675691.dkr.ecr.ap-northeast-2.amazonaws.com/tori-app/frontend-service:latest
```

### 4.4 ALB (Application Load Balancer)

```
인터넷에서 들어오는 트래픽을 분산

                    인터넷
                       │
                       ▼
              ┌─────────────┐
              │     ALB     │  ← AWS가 관리
              │ (xxx.elb.   │     고가용성 보장
              │  amazonaws) │     자동 SSL 가능
              └──────┬──────┘
                     │
         ┌──────────┬┴─────────┐
         ▼          ▼          ▼
    ┌────────┐ ┌────────┐ ┌────────┐
    │ Pod 1  │ │ Pod 2  │ │ Pod 3  │
    └────────┘ └────────┘ └────────┘

특징:
- L7 (HTTP/HTTPS) 로드밸런서
- URL 경로 기반 라우팅 가능 (/api → backend)
- 헬스 체크로 죽은 파드 자동 제외
```

### 4.5 EBS (Elastic Block Store)

```
MySQL 데이터를 저장하는 영구 디스크

┌─────────────┐          ┌─────────────┐
│   Pod       │          │    EBS      │
│  (MySQL)    │◀────────▶│   Volume    │
│             │  마운트   │   (10GB)    │
└─────────────┘          └─────────────┘

파드가 죽어도 데이터는 EBS에 안전하게 보관!
새 파드가 떠도 같은 EBS 연결 → 데이터 유지
```

---

## 5. HPA (자동 스케일링)

### 5.1 HPA란?

```
HPA = Horizontal Pod Autoscaler
      (수평적 파드 자동 확장기)

트래픽 적을 때:          트래픽 많을 때:
┌─────┐                 ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐
│Pod 1│                 │Pod 1│ │Pod 2│ │Pod 3│ │Pod 4│
└─────┘                 └─────┘ └─────┘ └─────┘ └─────┘
CPU: 30%                CPU: 70% 이상이면 자동 확장!

↔ 수평 확장: 파드 개수를 늘림
↕ 수직 확장: 파드 스펙을 올림 (CPU, 메모리)
```

### 5.2 우리 프로젝트 HPA 설정

```yaml
# k8s/hpa/hpa.yaml

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: frontend-service-hpa
  namespace: tori-app
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: frontend-service      # 이 Deployment를 스케일링

  minReplicas: 2                # 최소 2개 유지
  maxReplicas: 10               # 최대 10개까지

  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70    # CPU 70% 넘으면 확장
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80    # 메모리 80% 넘으면 확장

  behavior:
    scaleUp:
      stabilizationWindowSeconds: 0   # 즉시 확장
      policies:
        - type: Percent
          value: 100                  # 100%씩 늘림 (2→4→8)
          periodSeconds: 15
    scaleDown:
      stabilizationWindowSeconds: 300  # 5분 대기 후 축소
      policies:
        - type: Percent
          value: 10                    # 10%씩 줄임
          periodSeconds: 60
```

### 5.3 HPA 동작 시나리오

```
[시나리오: 점심시간 주문 폭주]

12:00 - 평상시
┌─────────┐ ┌─────────┐
│ order-1 │ │ order-2 │  CPU: 40%
└─────────┘ └─────────┘

12:15 - 주문 증가
┌─────────┐ ┌─────────┐
│ order-1 │ │ order-2 │  CPU: 75% ⚠️ 70% 초과!
└─────────┘ └─────────┘
        │
        ▼ HPA: "확장해야겠다!"

12:16 - 자동 확장
┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐
│ order-1 │ │ order-2 │ │ order-3 │ │ order-4 │
└─────────┘ └─────────┘ └─────────┘ └─────────┘
                                    CPU: 50%로 분산

13:30 - 점심 끝, 트래픽 감소
CPU: 25%... 5분간 유지

13:35 - HPA: "축소해도 되겠다"
┌─────────┐ ┌─────────┐ ┌─────────┐
│ order-1 │ │ order-2 │ │ order-3 │  10%씩 천천히 축소
└─────────┘ └─────────┘ └─────────┘
```

### 5.4 서비스별 HPA 설정 이유

| 서비스 | min | max | 이유 |
|--------|-----|-----|------|
| frontend | 2 | 10 | 사용자 직접 접속, 트래픽 변동 큼 |
| gateway | 2 | 10 | 모든 API 통과, 병목 가능 |
| order | 2 | 10 | 주문 피크 시 급증 |
| member | 2 | 8 | 로그인은 상대적으로 적음 |
| product | 2 | 8 | 읽기 위주, 캐싱 가능 |
| inventory | 2 | 8 | 주문당 1번 호출 |
| board | 2 | 6 | 공지사항은 트래픽 적음 |
| admin | 2 | 6 | 관리자만 사용 |

---

## 6. Ingress와 로드밸런싱

### 6.1 Ingress란?

```
클러스터 외부 → 내부로 들어오는 HTTP 트래픽을 관리

[Ingress 없이]                    [Ingress 있으면]

  사용자                            사용자
    │                                │
    ▼                                ▼
┌────────┐                      ┌─────────┐
│ LB 1   │─→ frontend           │ Ingress │─→ / → frontend
├────────┤                      │  (ALB)  │─→ /api → gateway
│ LB 2   │─→ gateway            │         │─→ /eureka → eureka
├────────┤                      └─────────┘
│ LB 3   │─→ eureka                  │
└────────┘                      로드밸런서 1개로 해결!
                                비용 절약 💰
```

### 6.2 우리 프로젝트 Ingress 설정

```yaml
# k8s/ingress.yaml

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tori-app-ingress
  namespace: tori-app
  annotations:
    # AWS ALB를 사용하겠다
    kubernetes.io/ingress.class: alb

    # 인터넷에서 접근 가능하게
    alb.ingress.kubernetes.io/scheme: internet-facing

    # 파드 IP로 직접 연결 (빠름)
    alb.ingress.kubernetes.io/target-type: ip

    # 헬스 체크 설정
    alb.ingress.kubernetes.io/healthcheck-path: /actuator/health
    alb.ingress.kubernetes.io/healthcheck-interval-seconds: '15'

spec:
  ingressClassName: alb
  rules:
    - http:
        paths:
          # / 로 시작하는 요청 → frontend
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 8005

          # /api 로 시작하는 요청 → gateway
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: gateway-service
                port:
                  number: 8000

          # /eureka 로 시작하는 요청 → eureka
          - path: /eureka
            pathType: Prefix
            backend:
              service:
                name: eureka-server
                port:
                  number: 8761
```

### 6.3 실제 URL 라우팅

```
http://k8s-toriapp-xxxxx.elb.amazonaws.com

요청 URL                          →  라우팅 대상
─────────────────────────────────────────────────
/                                 →  frontend-service:8005
/menu/drink                       →  frontend-service:8005
/login                            →  frontend-service:8005
/api/products                     →  gateway-service:8000
/api/orders                       →  gateway-service:8000
/api/auth/login                   →  gateway-service:8000
/eureka                           →  eureka-server:8761
```

---

## 7. 서비스 간 통신

### 7.1 동기 통신 (REST API)

```
[주문 조회 요청 흐름]

사용자 브라우저
      │
      │ GET /order/history
      ▼
┌─────────────┐
│  frontend   │
│  service    │
└──────┬──────┘
       │ REST API 호출
       │ GET http://gateway-service:8000/api/orders/history
       ▼
┌─────────────┐
│   gateway   │
│   service   │
└──────┬──────┘
       │ 라우팅
       │ GET http://order-service:8002/api/orders/history
       ▼
┌─────────────┐
│    order    │
│   service   │
└──────┬──────┘
       │ DB 조회
       ▼
┌─────────────┐
│  order_db   │
└─────────────┘
```

### 7.2 비동기 통신 (RabbitMQ)

```
[주문 완료 → 재고 차감]

동기로 하면? 🤔
─────────────────
order-service: "재고 차감해줘"
       │
       ▼ (대기...)
inventory-service: "잠깐만... DB 느려..."
       │
       ▼ (3초 후)
order-service: "아 드디어 응답왔다"
사용자: "왜 이렇게 느려?" 😠

비동기로 하면! 😊
─────────────────
order-service: "재고 차감해줘" → [RabbitMQ] → "메시지 넣어둘게"
       │                              │
       ▼ (즉시 응답)                    ▼ (나중에 처리)
사용자: "주문 완료!" 🎉          inventory-service: "오 메시지 왔네, 처리할게"
```

```
[RabbitMQ 메시지 흐름]

┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│    order    │         │  RabbitMQ   │         │  inventory  │
│   service   │────────▶│             │────────▶│   service   │
└─────────────┘ publish │ order.queue │ consume └─────────────┘
                        └─────────────┘

메시지 내용:
{
  "orderId": 123,
  "items": [
    {"productId": 1, "quantity": 2},
    {"productId": 5, "quantity": 1}
  ]
}

inventory-service가 받아서:
→ 아메리카노 원두 -40g
→ 라떼 우유 -200ml
```

### 7.3 서비스 디스커버리 (Eureka)

```
서비스들이 서로를 어떻게 찾을까?

[Eureka Server]
      │
      │ 등록 (register)
      │
┌─────┴─────┐
│           │
▼           ▼
member      product
service     service
(8004)      (8001)

1. 각 서비스가 Eureka에 자신을 등록
2. 다른 서비스 찾을 때 Eureka에 물어봄
3. Eureka가 "product-service는 10.0.0.15:8001 이야" 알려줌

K8s에서는?
→ K8s Service + DNS로 대체 가능
→ 하지만 Eureka도 함께 사용 (Spring Cloud 호환성)
```

---

## 8. 리소스 관리

### 8.1 requests vs limits

```yaml
resources:
  requests:          # "최소 이만큼은 필요해요"
    memory: "256Mi"  # 256 MiB 메모리
    cpu: "250m"      # 0.25 CPU 코어 (1000m = 1코어)
  limits:            # "최대 이만큼만 쓸게요"
    memory: "384Mi"
    cpu: "500m"
```

```
[스케줄링 시]

Node A (4GB 메모리)
├── 사용 중: 3GB
└── 남은 공간: 1GB

새 파드 (requests: 512Mi)
→ Node A에 배치 가능! ✅

새 파드 (requests: 2GB)
→ Node A에 배치 불가 ❌
→ 다른 노드 찾거나, 노드 추가 필요
```

### 8.2 우리 프로젝트 리소스 할당

| 서비스 | CPU (req/limit) | Memory (req/limit) | 이유 |
|--------|-----------------|---------------------|------|
| 일반 서비스 | 250m / 500m | 256Mi / 384Mi | Spring Boot 기본 |
| MySQL | 250m / 500m | 512Mi / 768Mi | DB는 메모리 많이 사용 |
| RabbitMQ | 250m / 500m | 512Mi / 768Mi | 메시지 버퍼링 |
| Eureka | 250m / 500m | 384Mi / 512Mi | 서비스 목록 캐싱 |

### 8.3 OOMKilled (메모리 부족)

```
파드가 limits를 초과하면?

┌─────────────────────────────────┐
│ Pod: order-service              │
│ Memory Limit: 384Mi            │
│                                 │
│ 현재 사용: 380Mi... 390Mi! 💥   │
└─────────────────────────────────┘
           │
           ▼
    OOMKilled (Out Of Memory)
    → 파드 강제 종료
    → K8s가 자동으로 새 파드 생성

확인 방법:
kubectl describe pod <파드명> -n tori-app
→ Last State: Terminated
→ Reason: OOMKilled
```

---

## 9. 데이터 영속성 (PV/PVC)

### 9.1 PV와 PVC란?

```
PV (Persistent Volume)
= 실제 물리적 저장소 (EBS 볼륨)
= "창고"

PVC (Persistent Volume Claim)
= 저장소 요청서
= "창고 10GB 빌려주세요"

┌─────────────────────────────────────────────┐
│                Kubernetes                    │
│                                             │
│  ┌─────────────┐      ┌─────────────┐      │
│  │    PVC      │      │     PV      │      │
│  │ "10GB 필요"  │─────▶│ "10GB EBS"  │      │
│  └──────┬──────┘      └──────┬──────┘      │
│         │                    │             │
│         │                    ▼             │
│         │            ┌─────────────┐       │
│         │            │ AWS EBS     │       │
│         │            │ 실제 디스크   │       │
│         │            └─────────────┘       │
│         ▼                                  │
│  ┌─────────────┐                           │
│  │    Pod      │                           │
│  │   (MySQL)   │ ← /var/lib/mysql에 마운트  │
│  └─────────────┘                           │
└─────────────────────────────────────────────┘
```

### 9.2 StatefulSet에서 PVC

```yaml
# mysql-product.yaml

volumeClaimTemplates:     # StatefulSet 전용
  - metadata:
      name: mysql-storage
    spec:
      accessModes: ["ReadWriteOnce"]   # 하나의 파드만 쓰기 가능
      storageClassName: gp3            # AWS EBS gp3 타입
      resources:
        requests:
          storage: 10Gi                # 10GB 요청
```

```
[파드가 죽었을 때]

Before:
┌──────────────────┐     ┌──────────────┐
│ mysql-product-0  │────▶│ EBS Volume   │
│   IP: 10.0.0.15  │     │  (10GB)      │
└──────────────────┘     │  데이터 있음   │
        ❌ 죽음!          └──────────────┘

After:
┌──────────────────┐     ┌──────────────┐
│ mysql-product-0  │────▶│ EBS Volume   │
│   IP: 10.0.0.99  │     │  (10GB)      │  ← 같은 볼륨!
│   (새로 생성)     │     │  데이터 유지!  │
└──────────────────┘     └──────────────┘

IP는 바뀌어도 데이터는 안전! 🎉
```

---

## 10. ConfigMap과 Secret

### 10.1 ConfigMap (설정값)

```yaml
# k8s/base/configmap.yaml

apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: tori-app
data:
  # 서비스 URL들 (민감하지 않음)
  EUREKA_SERVER_URL: "http://eureka-server:8761/eureka"
  SERVICE_MEMBER_URL: "http://member-service:8004"
  SERVICE_PRODUCT_URL: "http://product-service:8001"
  SERVICE_ORDER_URL: "http://order-service:8002"

  # 설정값
  EUREKA_INSTANCE_PREFER_IP_ADDRESS: "true"
```

**사용 방법:**
```yaml
# Deployment에서

env:
  - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
    valueFrom:
      configMapKeyRef:
        name: app-config
        key: EUREKA_SERVER_URL
```

### 10.2 Secret (비밀값)

```yaml
# k8s/base/secrets.yaml

apiVersion: v1
kind: Secret
metadata:
  name: db-secrets
  namespace: tori-app
type: Opaque
stringData:                          # 평문으로 작성
  MYSQL_MEMBER_PASSWORD: "password123"
  JWT_SECRET: "super-secret-key"

# K8s가 자동으로 Base64 인코딩
# 저장될 때: cGFzc3dvcmQxMjM=
```

**왜 분리하는가?**

```
[ConfigMap]                    [Secret]
- Git에 올려도 됨 ✅            - Git에 올리면 안됨 ❌
- 누구나 봐도 됨               - 암호화 저장
- URL, 포트, 설정              - 비밀번호, API Key, 토큰

우리 프로젝트:
secrets.yaml → Git에는 더미값
secrets-real.yaml → 별도 보관 (k8s/scripts/backup/)
```

### 10.3 초기 데이터 설정 (data.sql)

**Spring Boot의 data.sql 자동 실행**
```
Spring Boot 시작 시 src/main/resources/data.sql 파일이 자동 실행됨
→ 메뉴, 카테고리, 재고 등 초기 데이터 삽입
```

**K8s 환경변수 설정**
```yaml
# k8s/services/product-service.yaml

env:
  - name: SPRING_SQL_INIT_MODE
    value: "always"    # 항상 data.sql 실행
```

| 값 | 동작 |
|----|------|
| `always` | 매번 실행 (개발/테스트용) |
| `embedded` | 내장 DB(H2)일 때만 실행 |
| `never` | 실행 안함 |

**INSERT vs INSERT IGNORE 차이**

```sql
-- 기존 방식 (문제 있음)
INSERT INTO menu (id, name, price) VALUES (1, '아메리카노', 4500);

-- 파드 재시작 시:
-- Error: Duplicate entry '1' for key 'PRIMARY'
-- → 파드 CrashLoopBackOff! 💥
```

```sql
-- 수정된 방식 (우리 프로젝트)
INSERT IGNORE INTO menu (id, name, price) VALUES (1, '아메리카노', 4500);

-- 파드 재시작 시:
-- 이미 있으면 무시하고 넘어감
-- → 정상 동작! ✅
```

**동작 비교**
```
[INSERT INTO]
첫 번째 실행: 성공 ✅
두 번째 실행: 실패 ❌ (중복 키 에러)

[INSERT IGNORE INTO]
첫 번째 실행: 성공 ✅
두 번째 실행: 성공 ✅ (중복은 무시)
세 번째 실행: 성공 ✅ (계속 무시)

∴ K8s처럼 파드가 자주 재시작되는 환경에서는
  INSERT IGNORE INTO 필수!
```

**우리 프로젝트 적용 예시**
```sql
-- product-service/src/main/resources/data.sql

INSERT IGNORE INTO category (category_id, name) VALUES (1, '커피');
INSERT IGNORE INTO category (category_id, name) VALUES (2, '음료');

INSERT IGNORE INTO menu (menu_id, category_id, name, price, description)
VALUES (1, 1, '아메리카노', 4500, '깊은 풍미의 에스프레소');

-- inventory-service/src/main/resources/data.sql

INSERT IGNORE INTO material_master (material_id, name, unit, stock)
VALUES (1, '원두', 'g', 10000);
INSERT IGNORE INTO material_master (material_id, name, unit, stock)
VALUES (2, '우유', 'ml', 20000);
```

**관련 트러블슈팅**
```
증상: 메뉴 페이지에 데이터가 안 보임

확인 1: SPRING_SQL_INIT_MODE 확인
kubectl exec <파드명> -n tori-app -- env | grep SQL
→ "always"여야 함

확인 2: data.sql 문법 확인
→ INSERT IGNORE INTO 사용했는지?

확인 3: MySQL에서 직접 확인
kubectl exec -it mysql-product-0 -n tori-app -- mysql -u root -prootpass product_db -e "SELECT * FROM menu;"
```

---

## 11. 헬스 체크 (Probe)

### 11.1 세 가지 Probe

```
┌─────────────────────────────────────────────────────┐
│                    Pod                              │
│  ┌───────────────────────────────────────────────┐ │
│  │              Container                         │ │
│  │                                               │ │
│  │   livenessProbe    "살아있니?"                │ │
│  │        │           → 실패 시 컨테이너 재시작    │ │
│  │        ▼                                      │ │
│  │   ┌─────────┐                                 │ │
│  │   │   App   │                                 │ │
│  │   └─────────┘                                 │ │
│  │        ▲                                      │ │
│  │        │                                      │ │
│  │   readinessProbe   "준비됐니?"                │ │
│  │                    → 실패 시 트래픽 안보냄      │ │
│  │                                               │ │
│  │   startupProbe     "시작됐니?"                │ │
│  │                    → 시작 중에는 다른 체크 중지 │ │
│  └───────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

### 11.2 우리 프로젝트 설정

```yaml
# frontend-service.yaml

livenessProbe:
  httpGet:
    path: /                   # 이 URL로 체크
    port: 8005
  initialDelaySeconds: 60     # 시작 후 60초 대기
  periodSeconds: 10           # 10초마다 체크

readinessProbe:
  httpGet:
    path: /
    port: 8005
  initialDelaySeconds: 30     # 시작 후 30초 대기
  periodSeconds: 5            # 5초마다 체크
```

### 11.3 실제 시나리오

```
[시나리오: 서비스 배포]

0초:  새 파드 생성
      livenessProbe: 대기 (initialDelaySeconds: 60)
      readinessProbe: 대기 (initialDelaySeconds: 30)

30초: Spring Boot 아직 시작 중...
      readinessProbe 시작 → 실패 (아직 준비 안됨)
      → 트래픽 안보냄 ✅

45초: Spring Boot 시작 완료!
      readinessProbe → 성공!
      → 트래픽 받기 시작 ✅

60초: livenessProbe 시작 → 성공!
      → 계속 모니터링

300초: 앱 내부 데드락 발생! 😱
       livenessProbe → 실패!
       → K8s가 컨테이너 재시작
       → 자동 복구! 🎉
```

---

## 12. 실제 트래픽 흐름

### 12.1 전체 흐름도

```
[사용자가 아메리카노 주문하는 과정]

1. 브라우저에서 주문 버튼 클릭
   │
   ▼
2. ALB (k8s-toriapp-xxx.elb.amazonaws.com)
   │ /api/order/place
   ▼
3. Ingress 규칙 확인
   │ path: /api → gateway-service
   ▼
4. gateway-service (8000)
   │ 라우팅: /api/order/** → order-service
   ▼
5. order-service (8002)
   │ ├─ 주문 데이터 저장 → order_db
   │ └─ 재고 차감 요청 → RabbitMQ
   ▼
6. RabbitMQ
   │ order.queue에 메시지 저장
   ▼
7. inventory-service (8008)
   │ 메시지 수신 → 재고 차감 → inventory_db
   ▼
8. 완료!
```

### 12.2 DNS 해석 과정

```
frontend-service 에서 gateway-service 호출할 때:

http://gateway-service:8000/api/products

1. gateway-service 이름으로 DNS 조회
   │
   ▼
2. CoreDNS (K8s 내부 DNS)
   "gateway-service.tori-app.svc.cluster.local"
   → 10.100.50.30 (ClusterIP)
   │
   ▼
3. kube-proxy가 실제 파드로 분산
   → 10.0.1.15:8000 (Pod 1)
   → 10.0.2.20:8000 (Pod 2)
```

### 12.3 장애 발생 시 동작

```
[시나리오: order-service 파드 1개 죽음]

Before (정상):
         ┌───────────┐
         │   ALB     │
         └─────┬─────┘
               │
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌───────┐ ┌───────┐ ┌───────┐
│order-1│ │order-2│ │order-3│  모두 정상
└───────┘ └───────┘ └───────┘

After (order-2 죽음):
         ┌───────────┐
         │   ALB     │
         └─────┬─────┘
               │
    ┌──────────┼──────────┐
    ▼          ▼          ▼
┌───────┐ ┌───────┐ ┌───────┐
│order-1│ │order-2│ │order-3│
│  ✅   │ │  ❌   │ │  ✅   │
└───────┘ └───────┘ └───────┘

1. livenessProbe 실패 감지 (10초)
2. order-2 파드 재시작 시도
3. readinessProbe 실패 → 트래픽 제외
4. ALB는 order-1, order-3에만 트래픽 전송
5. 사용자는 장애를 느끼지 못함! 🎉
6. order-2 복구 완료 → 다시 트래픽 수신
```

---

## 부록: 자주 쓰는 명령어

```bash
# 파드 상태 확인
kubectl get pods -n tori-app

# 파드 상세 정보 (이벤트, 에러 확인)
kubectl describe pod <파드명> -n tori-app

# 파드 로그 보기
kubectl logs <파드명> -n tori-app
kubectl logs -f <파드명> -n tori-app  # 실시간

# HPA 상태 확인
kubectl get hpa -n tori-app

# 리소스 사용량 확인
kubectl top pods -n tori-app
kubectl top nodes

# 서비스 확인
kubectl get svc -n tori-app

# Ingress 확인
kubectl get ingress -n tori-app

# ConfigMap/Secret 확인
kubectl get configmap -n tori-app
kubectl get secret -n tori-app

# MySQL 접속
kubectl exec -it mysql-product-0 -n tori-app -- mysql -u root -p

# 파드 내부 접속
kubectl exec -it <파드명> -n tori-app -- sh
```

---

## 13. 운영 실무 가이드

### 13.1 DB 비밀번호 변경하기

**처음 배포할 때 (데이터 없음)**
```powershell
# secrets.yaml 수정 후 적용하면 끝
kubectl apply -f k8s/base/secrets.yaml
kubectl apply -f k8s/mysql/
```

**이미 운영 중일 때 (데이터 있음)**
```powershell
# 1. secrets.yaml에 새 비밀번호 설정
# 2. Secret 업데이트
kubectl apply -f k8s/base/secrets.yaml

# 3. MySQL 접속해서 직접 비밀번호 변경
kubectl exec -it mysql-product-0 -n tori-app -- mysql -u root -p기존비번

# MySQL 내부에서 실행:
ALTER USER 'root'@'%' IDENTIFIED BY '새비밀번호';
ALTER USER 'product_user'@'%' IDENTIFIED BY '새비밀번호';
FLUSH PRIVILEGES;
EXIT;

# 4. 애플리케이션 서비스 재시작
kubectl rollout restart deployment/product-service -n tori-app
```

**왜 이렇게 복잡한가?**
```
MySQL 최초 실행 시:
PVC 비어있음 → 환경변수로 비밀번호 설정 → PVC에 저장

MySQL 재시작 시:
PVC에 데이터 있음 → 환경변수 무시! → PVC의 비밀번호 사용

∴ 운영 중에는 MySQL 내부에서 직접 변경해야 함
```

### 13.2 코드 수정 후 재배포

```powershell
# 1. 코드 수정 후 빌드
./gradlew :frontend-service:clean :frontend-service:build -x test

# 2. Docker 이미지 빌드
docker build --no-cache -t frontend-service -f frontend-service/Dockerfile .

# 3. ECR에 푸시
aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com

docker tag frontend-service:latest 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com/tori-app/frontend-service:latest
docker push 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com/tori-app/frontend-service:latest

# 4. K8s 재배포 (새 이미지 pull)
kubectl rollout restart deployment/frontend-service -n tori-app

# 5. 배포 상태 확인
kubectl rollout status deployment/frontend-service -n tori-app
```

### 13.3 롤백 (이전 버전으로 되돌리기)

```powershell
# 배포 히스토리 확인
kubectl rollout history deployment/frontend-service -n tori-app

# 결과 예시:
# REVISION  CHANGE-CAUSE
# 1         <none>
# 2         <none>
# 3         <none>  ← 현재

# 바로 이전 버전으로 롤백
kubectl rollout undo deployment/frontend-service -n tori-app

# 특정 버전으로 롤백
kubectl rollout undo deployment/frontend-service -n tori-app --to-revision=1
```

```
[롤백 동작 원리]

현재 (v3):
┌─────┐ ┌─────┐
│v3-1 │ │v3-2 │
└─────┘ └─────┘

롤백 실행:
┌─────┐ ┌─────┐ ┌─────┐
│v3-1 │ │v3-2 │ │v2-1 │ ← 새 파드 생성 (이전 이미지)
└─────┘ └─────┘ └─────┘

롤백 완료:
┌─────┐ ┌─────┐
│v2-1 │ │v2-2 │ ← v3 파드들 종료됨
└─────┘ └─────┘
```

### 13.4 수동 스케일링

```powershell
# 파드 개수 직접 조절
kubectl scale deployment/order-service -n tori-app --replicas=5

# 노드 개수 조절 (EC2 인스턴스)
eksctl scale nodegroup --cluster=teamproject-cluster --name=tori-nodes --nodes=3 --region=ap-northeast-2

# HPA 일시 비활성화 (수동 제어하고 싶을 때)
kubectl delete hpa order-service-hpa -n tori-app

# HPA 다시 활성화
kubectl apply -f k8s/hpa/hpa.yaml
```

### 13.5 로그 확인 및 디버깅

```powershell
# 실시간 로그 보기
kubectl logs -f deployment/order-service -n tori-app

# 이전 파드 로그 (재시작된 경우)
kubectl logs <파드명> -n tori-app --previous

# 여러 파드 로그 동시에 (라벨로 선택)
kubectl logs -l app=order-service -n tori-app --all-containers

# 최근 100줄만
kubectl logs <파드명> -n tori-app --tail=100

# 특정 시간 이후 로그
kubectl logs <파드명> -n tori-app --since=1h

# 파드 내부 접속해서 디버깅
kubectl exec -it <파드명> -n tori-app -- sh

# 파드 상세 정보 (이벤트, 에러 원인)
kubectl describe pod <파드명> -n tori-app

# 최근 이벤트 확인
kubectl get events -n tori-app --sort-by='.lastTimestamp' | tail -20
```

### 13.6 데이터 백업/복구

**MySQL 백업**
```powershell
# 백업 (로컬에 SQL 파일로 저장)
kubectl exec mysql-product-0 -n tori-app -- mysqldump -u root -prootpass product_db > backup_product_$(date +%Y%m%d).sql

# 모든 DB 백업
kubectl exec mysql-product-0 -n tori-app -- mysqldump -u root -prootpass product_db > backup_product.sql
kubectl exec mysql-order-0 -n tori-app -- mysqldump -u root -prootpass order_db > backup_order.sql
kubectl exec mysql-member-0 -n tori-app -- mysqldump -u root -prootpass member_db > backup_member.sql
kubectl exec mysql-inventory-0 -n tori-app -- mysqldump -u root -prootpass inventory_db > backup_inventory.sql
```

**MySQL 복구**
```powershell
# 복구 (SQL 파일에서)
kubectl exec -i mysql-product-0 -n tori-app -- mysql -u root -prootpass product_db < backup_product.sql
```

**PVC 스냅샷 (AWS EBS)**
```powershell
# EBS 볼륨 ID 확인
kubectl get pv -n tori-app

# AWS 콘솔 또는 CLI로 EBS 스냅샷 생성
aws ec2 create-snapshot --volume-id vol-xxxxxxxxx --description "MySQL backup"
```

### 13.7 도메인/SSL 설정

**Route 53으로 도메인 연결**
```
1. Route 53에서 도메인 구매 또는 등록
2. Hosted Zone 생성
3. A 레코드 추가:
   - 이름: tori-coffee.com
   - 유형: A - Alias
   - 값: ALB DNS 이름 (k8s-toriapp-xxx.elb.amazonaws.com)
```

**HTTPS 설정 (ACM + ALB)**
```yaml
# ingress.yaml에 추가

metadata:
  annotations:
    # ACM 인증서 ARN
    alb.ingress.kubernetes.io/certificate-arn: arn:aws:acm:ap-northeast-2:490866675691:certificate/xxxxx

    # HTTP → HTTPS 리다이렉트
    alb.ingress.kubernetes.io/ssl-redirect: '443'

    # HTTPS 포트
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS": 443}]'
```

### 13.8 비용 최적화

**사용하지 않을 때 비용 절약**
```powershell
# 방법 1: 노드만 0개로 (빠른 재시작 가능, $105/월)
eksctl scale nodegroup --cluster=teamproject-cluster --name=tori-nodes --nodes=0 --nodes-min=0 --region=ap-northeast-2

# 방법 2: 클러스터 완전 삭제 ($0/월, 재생성 15-20분)
eksctl delete cluster --name teamproject-cluster --region ap-northeast-2
```

**Spot 인스턴스 사용 (70% 저렴)**
```yaml
# cluster-config.yaml

managedNodeGroups:
  - name: tori-nodes-spot
    instanceType: t3.medium
    spot: true              # Spot 인스턴스 사용
    desiredCapacity: 5
```

**리소스 최적화**
```yaml
# 실제 사용량 확인 후 조절
kubectl top pods -n tori-app

# 사용량이 낮으면 requests/limits 줄이기
resources:
  requests:
    memory: "192Mi"    # 256Mi → 192Mi
    cpu: "200m"        # 250m → 200m
```

### 13.9 보안 체크리스트

**하지 말아야 할 것**
```
❌ secrets.yaml에 실제 비밀번호 넣고 Git 푸시
❌ AWS Access Key를 코드에 하드코딩
❌ MySQL을 외부에 노출 (LoadBalancer 타입 사용)
❌ root 사용자로 앱 실행
❌ 모든 포트 오픈
```

**해야 할 것**
```
✅ Secret으로 비밀번호 관리
✅ IAM Role 사용 (Access Key 대신)
✅ MySQL은 ClusterIP로 내부만 접근
✅ 최소 권한 원칙 (필요한 권한만)
✅ 네트워크 정책으로 트래픽 제한
✅ 정기적인 보안 업데이트
```

**Secret 암호화 확인**
```powershell
# Secret이 Base64 인코딩되어 저장됨
kubectl get secret db-secrets -n tori-app -o yaml

# 디코딩해서 확인 (관리자만)
kubectl get secret db-secrets -n tori-app -o jsonpath='{.data.MYSQL_PRODUCT_PASSWORD}' | base64 -d
```

---

## 14. 트러블슈팅 가이드

### 14.1 파드가 Pending 상태

```powershell
kubectl describe pod <파드명> -n tori-app
```

| 에러 메시지 | 원인 | 해결 |
|------------|------|------|
| `Insufficient cpu` | 노드 리소스 부족 | 노드 추가 또는 requests 줄이기 |
| `Insufficient memory` | 메모리 부족 | 노드 추가 또는 requests 줄이기 |
| `no nodes available` | 노드가 없음 | 노드 스케일업 |
| `volume node affinity conflict` | PVC가 다른 AZ에 있음 | PVC 삭제 후 재생성 |

### 14.2 파드가 CrashLoopBackOff 상태

```powershell
# 로그 확인
kubectl logs <파드명> -n tori-app --previous
```

| 원인 | 해결 |
|------|------|
| DB 연결 실패 | MySQL 파드가 Running인지 확인 |
| 환경변수 누락 | ConfigMap/Secret 확인 |
| 포트 충돌 | containerPort 확인 |
| OOMKilled | memory limit 늘리기 |

### 14.3 서비스 연결 안됨

```powershell
# 서비스 확인
kubectl get svc -n tori-app

# 엔드포인트 확인 (파드와 연결됐는지)
kubectl get endpoints <서비스명> -n tori-app

# DNS 확인 (파드 내부에서)
kubectl exec -it <파드명> -n tori-app -- nslookup gateway-service
```

### 14.4 Ingress/ALB 문제

```powershell
# Ingress 상태 확인
kubectl describe ingress tori-app-ingress -n tori-app

# ALB Controller 로그 확인
kubectl logs -n kube-system -l app.kubernetes.io/name=aws-load-balancer-controller
```

| 증상 | 원인 | 해결 |
|------|------|------|
| ADDRESS가 비어있음 | ALB Controller 미설치 | Helm으로 설치 |
| 502 Bad Gateway | 백엔드 파드 없음 | 파드 상태 확인 |
| 503 Service Unavailable | 헬스체크 실패 | readinessProbe 확인 |
| 404 Not Found | 경로 매칭 안됨 | Ingress rules 확인 |

### 14.5 이미지 Pull 실패

```powershell
# 에러: ImagePullBackOff 또는 ErrImagePull

# ECR 로그인 상태 확인
aws ecr get-login-password --region ap-northeast-2 | docker login --username AWS --password-stdin 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com

# 이미지 존재 확인
aws ecr describe-images --repository-name tori-app/frontend-service --region ap-northeast-2

# 이미지 태그 확인
kubectl describe pod <파드명> -n tori-app | grep Image
```

### 14.6 MySQL 데이터 안보임

```powershell
# 1. data.sql 실행 여부 확인
kubectl logs <product-service-파드> -n tori-app | grep -i "sql"

# 2. 환경변수 확인
kubectl exec <product-service-파드> -n tori-app -- env | grep SQL
# SPRING_SQL_INIT_MODE=always 여야 함

# 3. MySQL에서 직접 확인
kubectl exec -it mysql-product-0 -n tori-app -- mysql -u root -prootpass product_db -e "SELECT COUNT(*) FROM menu;"

# 4. 수동으로 data.sql 실행
kubectl exec -i mysql-product-0 -n tori-app -- mysql -u root -prootpass product_db < product-service/src/main/resources/data.sql
```

### 14.7 메모리 부족 (OOMKilled)

```powershell
# OOM 확인
kubectl describe pod <파드명> -n tori-app | grep -A5 "Last State"

# 해결: limits 늘리기
resources:
  limits:
    memory: "512Mi"  # 384Mi → 512Mi
```

### 14.8 노드 NotReady 상태

```powershell
# 노드 상태 확인
kubectl describe node <노드명>

# 일반적인 원인:
# - 디스크 부족: kubelet이 중지됨
# - 네트워크 문제: API Server와 통신 안됨
# - 리소스 고갈: CPU/메모리 부족

# AWS 콘솔에서 EC2 인스턴스 상태 확인
# 필요시 인스턴스 재부팅 또는 교체
```

---

## 15. CI/CD 파이프라인 (참고)

### 15.1 GitHub Actions 예시

```yaml
# .github/workflows/deploy.yml

name: Deploy to EKS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      - name: Login to ECR
        run: |
          aws ecr get-login-password | docker login --username AWS --password-stdin 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com

      - name: Build and Push
        run: |
          ./gradlew :frontend-service:build -x test
          docker build -t frontend-service -f frontend-service/Dockerfile .
          docker tag frontend-service:latest 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com/tori-app/frontend-service:latest
          docker push 490866675691.dkr.ecr.ap-northeast-2.amazonaws.com/tori-app/frontend-service:latest

      - name: Deploy to EKS
        run: |
          aws eks update-kubeconfig --name teamproject-cluster --region ap-northeast-2
          kubectl rollout restart deployment/frontend-service -n tori-app
```

### 15.2 배포 자동화 흐름

```
[CI/CD 파이프라인]

개발자가 코드 푸시
       │
       ▼
┌─────────────────┐
│  GitHub Actions │
│  트리거 발생     │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   빌드 & 테스트   │
│  ./gradlew build │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Docker 이미지   │
│  빌드 & ECR 푸시 │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   K8s 배포      │
│ rollout restart │
└────────┬────────┘
         │
         ▼
    배포 완료! 🎉
```

---

## 마무리

이 문서에서 다룬 핵심 개념:

| 개념 | 한 줄 요약 |
|------|----------|
| **MSA** | 서비스/DB를 분리해서 독립적으로 개발/배포/확장 |
| **Container** | 앱 + 환경을 패키징, 어디서든 동일하게 실행 |
| **Pod** | K8s 배포 최소 단위, 컨테이너 1개 이상 |
| **Deployment** | 파드 개수 유지, 자동 복구 |
| **Service** | 파드들에게 고정 IP/DNS 제공 |
| **HPA** | CPU/메모리 기반 자동 스케일링 |
| **Ingress** | 외부 트래픽을 내부 서비스로 라우팅 |
| **PV/PVC** | 파드가 죽어도 데이터 유지 |
| **ConfigMap** | 설정값 관리 (URL, 포트 등) |
| **Secret** | 비밀값 관리 (비밀번호, 토큰) |
| **Probe** | 헬스 체크로 자동 복구 |

---

*작성일: 2026-01-19*
*프로젝트: TORI COFFEE*